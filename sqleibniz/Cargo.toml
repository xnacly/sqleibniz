[package]
name = "sqleibniz"
version = "0.0.5"
edition = "2024"
description = "Static analysis and LSP for SQL in Rust"
repository = "https://github.com/xnacly/sqleibniz"
license = "MIT"
readme = "../README.md"

categories = ["development-tools"]
keywords = ["sql", "lsp", "static-analysis", "parser", "language-server"]

[features]
trace = ["sqleibniz-ast/trace"]
trace-analysis = []

[dependencies]
sqleibniz-ast = { version = "0.0.1", path = "../sqleibniz-ast", features = ["serde"] }

clap = { version = "4.6.2", features = ["derive"] }
lsp-server = "0.9.0"
lsp-types = "0.97.0"
mlua = { version = "0.12.0", features = ["lua54", "vendored"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
toml = "1.1.3"

[dev-dependencies]
pretty_assertions = "1.4.1"

[package.metadata.docs.rs]
no-default-features = true
